# Requirement

```bash
gcloud auth login
gcloud config set project PROJECT_ID
```

```bash
terraform init
terraform apply
terraform destroy
```

```bash
set -x GOOGLE_APPLICATION_CREDENTIALS PATH/TO/CREDENTIALS
```
